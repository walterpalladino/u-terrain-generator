using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PSRoad : MonoBehaviour
{

    [SerializeField]
    private PSRoadNode[] nodes;

    public PSRoadNode[] Nodes { get => nodes; set => nodes = value; }




    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
