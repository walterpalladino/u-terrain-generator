using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PSRoadNetwork : MonoBehaviour
{

    [SerializeField]
    private PSRoad[] roads;

    public PSRoad[] Roads { get => roads; set => roads = value; }




    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
